핵심 제약들 (게임플레이에 큰 영향)

  1. 시뮬레이션 콜백 대부분 비활성화

  corecontrol.py:149-157 — simTick() 내부에서 핵심 엔진 기능들이 주석 처리됨:
  #self.engine.cityEvaluation()   # 도시 평가 시스템
  #self.engine.updateDate()       # 날짜 진행 (세금 징수 주기 등)
  #self.engine.changeCensus()     # 인구 통계 업데이트
  #self.engine.simUpdate()        # 일반 시뮬레이션 업데이트
  #self.engine.doTimeStuff()      # 시간 기반 이벤트 (재난 등)
  이것들이 꺼져 있으면 세금 징수, 재난, 시간 경과 이벤트가 제대로 작동하지 않을 수 있음.

  2. 토지 가치 수집 비활성화

  corecontrol.py:236 — getLandValue() 호출이 주석 처리:
  #self.land_value += self.engine.getLandValue(im, jm)
  관측에도 리워드에도 반영 안 됨.

  3. 리워드 가중치 0으로 설정

  env.py:54-55:
  'num_plants': 0,     # 발전소 수 → 보상 기여 0
  'mayor_rating': 0,   # 시장 지지율 → 보상 기여 0

  4. getPopReward() 항상 0 반환

  env.py:410 — 복잡한 인구 보상 계산이 있지만 마지막에:
  return 0          # ← 항상 0!
  return pop_reward  # ← unreachable

  5. 자동 불도저 상시 활성화

  corecontrol.py:116 — self.engine.autoBulldoze = True
  에이전트가 기존 건물 위에 그냥 지을 수 있음. 철거 비용/전략 불필요.

  중간 제약들

  ┌────────────────────────┬───────────────────────────────────┬─────────────────────────────┐
  │          위치          │               제약                │            영향             │
  ├────────────────────────┼───────────────────────────────────┼─────────────────────────────┤
  │ corecontrol.py:51      │ setGameLevel(2) 고정              │ 난이도 조절 불가            │
  ├────────────────────────┼───────────────────────────────────┼─────────────────────────────┤
  │ corecontrol.py:128-129 │ setSpeed(3), setPasses(100) 고정  │ 시뮬레이션 속도/해상도 고정 │
  ├────────────────────────┼───────────────────────────────────┼─────────────────────────────┤
  │ corecontrol.py:89      │ Query 도구 제외                   │ 도시 정보 조회 불가         │
  ├────────────────────────┼───────────────────────────────────┼─────────────────────────────┤
  │ env.py:174             │ past_actions 추적 비활성화        │ 행동 이력 없음              │
  ├────────────────────────┼───────────────────────────────────┼─────────────────────────────┤
  │ env.py:478-511         │ 교통/도로 네트워크 보상 전체 주석 │ 도로 구축 인센티브 없음     │
  └────────────────────────┴───────────────────────────────────┴─────────────────────────────┘

  mayorl에서 추가로 활성화할 만한 것들

  자금 제약 다음으로 가장 임팩트가 큰 것들:

  1. cityEvaluation() / changeCensus() 활성화 — 세수 징수가 제대로 돌아가야 자금 관리가 의미있음. 현재 세수가 정상 작동하는지 확인 필요.
  2. 토지 가치를 observation에 추가 — 어디에 지어야 효율적인지 판단 근거
  3. mayor_rating 가중치 활성화 — 이미 mayorl reward에 반영했지만, 엔진의 cityEvaluation()이 꺼져있으면 rating 자체가 업데이트 안 될 수 있음
  4. autoBulldoze = False — 철거 비용까지 고려하게 하면 더 현실적

  특히 **1번(시뮬레이션 콜백)**은 mayorl의 세수 메커니즘과 직결됩니다. 이것들이 꺼져있으면 simTick() 후 자금 변화가 없어서 커리큘럼 Phase 2~3에서 세수 수입이 0일 수 있습니다. 확인해볼까요?






